# Current Program Information
- Fitness: {fitness_score}
- Feature coordinates: {feature_coords}
- Focus areas: {improvement_areas}

{artifacts}

# Program Evolution History
{evolution_history}

# Current Program
```{language}
{current_program}
````

# Task

You are a **diff-only generator**. Your first output **MUST** be one or more SEARCH/REPLACE blocks.

* Put the **diff blocks first**.
* After the diff blocks, you **may** add at most 5 lines of rationale.
* Do **not** wrap diff blocks in code fences.
* Do **not** output anything before the first `<<<<<<< SEARCH`.

## Output Format (Hard Constraints)

* One or more blocks in this exact form (no extra spaces on marker lines):


<<<<<<< SEARCH
# Original code to find and replace (must match exactly)
=======
# New replacement code
>>>>>>> REPLACE

* The SEARCH section **must exactly match** a contiguous region in the Current Program (including whitespace).
* You may output multiple such blocks. Separate blocks with a blank line.
* If you add explanations, place them **after** the last `>>>>>>> REPLACE`, not before, and keep them ≤5 lines.

## Example (Valid)

Current Program:

```python
"""Code optimization example for OpenEvolve"""
from typing import *
from bisect import *
from collections import *
from copy import *
from datetime import *
from heapq import *
from math import *
from re import *
from string import *
from random import *
from itertools import *
from functools import *
from operator import *

import string
import re
import datetime
import collections
import heapq
import bisect
import copy
import math
import random
import itertools
import functools
import operator

class TreeNode:
    def __init__(self, val=0, left=None, right=None, next=None):
        self.val = val
        self.left = left
        self.right = right
        self.next = next


class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

# EVOLVE-BLOCK-START
class Solution(object):
    def exist(self, board, word):
        """
        :type board: List[List[str]]
        :type word: str
        :rtype: bool
        """
# EVOLVE-BLOCK-END
```

Valid Change: 

<<<<<<< SEARCH
def exist(self, board, word):
    """
    :type board: List[List[str]]
    :type word: str
    :rtype: bool
    """
=======
def exist(self, board, word):
    """
    :type board: List[List[str]]
    :type word: str
    :rtype: bool
    return True
>>>>>>> REPLACE

Invalid Change(do not match original code exactly):

<<<<<<< SEARCH
def exist(self, board, word):
    """
    :type board: List[List[str]]
    :type word: str
    :rtype: bool
    """
    return False
=======
def exist(self, board, word):
    """
    :type board: List[List[str]]
    :type word: str
    :rtype: bool
    """
    return True
>>>>>>> REPLACE

## Goal

Suggest changes that improve FITNESS while maintaining diversity across: {feature_dimensions}.
Make **targeted** improvements only; do not rewrite the entire program.
Focus on `search_algorithm` when relevant.

## Self-Check (before emitting)

* [ ] The very first characters of the response are `<<<<<<< SEARCH`
* [ ] Every SEARCH region is copied verbatim from Current Program
* [ ] No code fences around diff blocks
* [ ] (Optional) Rationale appear **after** the last block

